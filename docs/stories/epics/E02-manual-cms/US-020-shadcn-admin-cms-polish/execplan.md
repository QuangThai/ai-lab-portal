# Exec Plan

1. Initialize shadcn in `frontend/` with path alias and editorial token mapping.
2. Add required UI primitives and shared admin styling helpers.
3. Refactor admin CMS pages/components to shadcn + shell.
4. Polish `/admin` dashboard to match CMS shell.
5. Run React Doctor, frontend checks, backend tests, E2E, harness verify.

Risk: normal — UI-only, authenticated routes unchanged at API layer.
